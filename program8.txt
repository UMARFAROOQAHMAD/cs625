class Hypot {
  public static void main (String args[]) { 
  double x,y,z;
    x = 3;
    y = 4;
    z = Math.sqrt(x*x + y*y);
    ci = im * im *im;
   system.out.prient("Hypotenuse is " +z);
}
}